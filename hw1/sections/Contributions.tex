\section{Group Members Contribution}

\begin{description}
	\item[Baldan Fabio (2203580)] has contributed to this project by:
    \begin{enumerate}
        \item Designing and implementing the resource model layer: \texttt{AbstractResource}, \texttt{User}, \texttt{Dish}, \texttt{Order}, \texttt{OrderDish}, \texttt{Category}, \texttt{Promotion}, \texttt{Message}, and \texttt{ResourceList}, including full JSON serialisation and deserialisation logic;
        \item Implementing \texttt{AbstractRR}, the shared base class for all REST resources, handling \texttt{Accept} and \texttt{Content-Type} validation, error envelope formatting, and the dispatch lifecycle;
        \item Implementing \texttt{EditUserRR} and the corresponding \texttt{EditUserDAO};
        \item Writing a Docker Compose file with Tomcat and all the necessary services, implementing Docker secrets for secure credential management;
        \item Designing and maintaining the database schema (\texttt{CREATE.sql}) through multiple revisions;
        \item Implementing the \texttt{AuthFlowFilter} servlet filter to redirect already-authenticated users.
    \end{enumerate}

	\item[Garberino Alvise (2196387)] has contributed to this project by:
    \begin{enumerate}
        \item Designing and maintaining the ER diagram (\texttt{ER.drawio}) and deriving the relational database schema from it, including foreign-key constraints and \texttt{ON DELETE} rules;
        \item Implementing all five Dish DAOs: \texttt{CreateDishDAO}, \texttt{DeleteDishDAO}, \texttt{EditDishDAO}, \texttt{GetDishDAO}, and \texttt{GetAllDishesDAO};
        \item Implementing the Ingredient read DAOs: \texttt{GetIngredientDAO} and \texttt{GetAllIngredientsDAO};
        \item Implementing the \texttt{RestResource} interface and the initial \texttt{RestDispatcherServlet} with basic HTTP route dispatching;
        \item Implementing \texttt{GetUserRR} and contributing to authentication cookie validation in the web filter.
    \end{enumerate}

	\item[Merja Klaudio (2197815)] has contributed to this project by:
    \begin{enumerate}
        \item Implementing \texttt{JWTUtil} for JWT generation, signing, and validation, and integrating it into the authentication flow;
        \item Implementing \texttt{AbstractDAO} and \texttt{ConnectionPoolSingleton}, providing the shared data-access foundation for all DAOs;
        \item Implementing the complete REST resource layer for Users (\texttt{RegisterUserRR}, \texttt{AuthenticateUserRR}, \texttt{LogoutUserRR}, \texttt{DeleteUserRR}), Categories (\texttt{CreateCategoryRR}, \texttt{DeleteCategoryRR}, \texttt{GetAllCategoriesRR}), Ingredients (\texttt{CreateIngredientRR}, \texttt{EditIngredientRR}, \texttt{DeleteIngredientRR}), and Dishes (\texttt{CreateDishRR}, \texttt{EditDishRR}, \texttt{GetDishRR}, \texttt{GetAllDishesRR});
        \item Implementing the corresponding Category DAOs: \texttt{CreateCategoryDAO}, \texttt{DeleteCategoryDAO}, and \texttt{GetAllCategoriesDAO};
        \item Structuring the project layout, managing Maven dependencies and plugins, and enforcing code style via the Spotless plugin.
    \end{enumerate}

	\item[Padoan Giancarlo (2188345)] has contributed to this project by:
    \begin{enumerate}
        \item Designing and implementing \texttt{ErrorCodes} and \texttt{Actions}, providing a unified taxonomy of REST error codes and action identifiers used throughout the application;
        \item Implementing \texttt{Validator} for server-side regex validation of email addresses and passwords;
        \item Implementing \texttt{RegisterUserDAO} and contributing to the \texttt{GetOrderDAO};
        \item Implementing the complete REST resource layer for Orders (\texttt{CreateOrderRR}, \texttt{DeleteOrderRR}, \texttt{EditOrderStatusRR}, \texttt{GetOrderRR}, \texttt{GetAllOrdersRR}, \texttt{GetUserOrdersRR}) and Promotions (\texttt{NewPromotionRR}, \texttt{GetPromotionRR}, \texttt{CheckPromotionRR}, \texttt{CheckUsageRR});
        \item Implementing all Promotion DAOs: \texttt{NewPromotionDAO}, \texttt{GetPromotionDAO}, \texttt{CheckPromotionDAO}, and \texttt{CheckPromotionUsageDAO};
        \item Implementing \texttt{DashboardFilter} and custom exception classes (\texttt{NotValidOrderException}, \texttt{MalformedJSONException}).
    \end{enumerate}

	\item[Sanavia Thomas (2197484)] has contributed to this project by:
    \begin{enumerate}
        \item Implementing the \texttt{DataAccessObject} base interface and \texttt{LogContext} for structured logging across the application;
        \item Implementing all six Order DAOs: \texttt{CreateOrderDAO}, \texttt{DeleteOrderDAO}, \texttt{EditOrderStatusDAO}, \texttt{GetOrderDAO}, \texttt{GetAllOrdersDAO}, and \texttt{GetUserOrdersDAO}, including transaction management for multi-step order creation;
        \item Implementing the centralised \texttt{Router} class for route registration and dispatch;
        \item Implementing \texttt{DashboardServlet}, \texttt{IngredientsServlet}, \texttt{ResourceList}, and the \texttt{UnexpectedKeyException} JSON exception;
        \item Bug-fixing and adjusting misconfigurations of the Docker Compose file, and contributing to \texttt{AbstractRR} refactoring (Content-Type handling and transaction flow);
        \item Writing the REST API documentation, covering the summary table and the detailed endpoint specifications for Orders and Promotions.
    \end{enumerate}
\end{description}
